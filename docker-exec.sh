#Exec command in a running container
#docker container exec -it redist /bin/bash
#-i keep input interactiv
#-t agr for alocate terminal access
#/bin/bash instance program code in container
docker exec -it redis bash
